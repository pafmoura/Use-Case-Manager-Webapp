<?xml version="1.0" encoding="UTF-8"?>
<root>
    <element1 attribute1 = "{{first}}">
    </element1>
    <element2 attribute1 = "{{second}}">
{% for d in data %}
        <value id="{{d.id}}">{{d.name}}</value>
{% endfor %}
    </element2>
</root>